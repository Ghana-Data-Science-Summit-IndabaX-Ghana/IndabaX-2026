# Read automatically by docker compose. Overrides defaults in docker-compose.yaml.
# AIRFLOW_UID: your host user id, so files written to mounted folders stay yours.
AIRFLOW_UID=501
# Pin the Airflow image/version here (change the tag if you want a different release).
AIRFLOW_IMAGE_NAME=apache/airflow:3.0.2
